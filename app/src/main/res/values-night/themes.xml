<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.StudyTrackerApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Set proper background and text contrast -->
        <item name="android:windowBackground">@color/background_color</item>
        <item name="android:textColor">@color/background_color</item>

        <!-- Material color overrides -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/background_color</item>
    </style>
</resources>
